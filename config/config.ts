const config = {
    port: 8443,
    ssl: false,

    title: `FreedomNetworkProxy`,
    prefix: `/astrox/`,
    codec: `xor`,
    blacklist: [`accounts.google.com`],
    smokeprefix: `/astrox2/`
};

export default config;
